'use strict';

var deriveContentTypeMcpTools = require('./derive-content-type-mcp-tools.js');
var index = require('../utils/index.js');

/**
 * Registers derived content-type MCP tools during plugin bootstrap, before the MCP HTTP server starts.
 */ const registerContentManagerMcpTools = async ({ strapi })=>{
    if (strapi.ai.mcp.isEnabled() !== true) {
        return;
    }
    const i18nPlugin = strapi.plugin('i18n');
    let localeCodes = null;
    let defaultLocale = null;
    if (i18nPlugin !== undefined) {
        localeCodes = (await i18nPlugin.service('locales').find()).map((locale)=>locale.code);
        defaultLocale = await i18nPlugin.service('locales').getDefaultLocale();
    }
    const models = index.getService('content-types').findDisplayedContentTypes();
    const tools = deriveContentTypeMcpTools.deriveDisplayedContentTypeMcpToolDefinitions(strapi, models, {
        localeCodes,
        defaultLocale
    });
    for (const tool of tools){
        strapi.ai.mcp.registerTool(tool);
    }
};

exports.registerContentManagerMcpTools = registerContentManagerMcpTools;
//# sourceMappingURL=register-content-manager-mcp-tools.js.map
